- [Flexa Market repo + Replit dev setup](flexa-market-setup.md) — repo is ~1.5GB (attached_assets); clone via GitHub blob API skipping assets; dev DB needs schema push (drop schema first to avoid drizzle rename prompts).
- [Expo SDK version updates & freshness gate](expo-version-updates.md) — bump mobile Expo deps with expo install --check; minimumReleaseAge:1440 blocks same-day SDK patch batches (exclude expo/'expo-*'/'@expo/*'/babel-preset-expo); --clear makes dev cold start trip port-detection.
- [Flexa Market email/SMS/payments config](flexa-comms-config.md) — login is email-OTP via Resend (RESEND_API_KEY); Twilio SMS is intentionally stubbed; Stripe uses Replit connector fallback.
- [Mobile build pnpm-exec project-root bug](mobile-build-pnpm-exec.md) — use expo bin directly in build.js, NOT pnpm exec expo, to keep Metro project root at artifacts/mobile.
- [FlexaMarket production API + OTA config](flexa-production-api.md) — stable API at bonjour-tool.replit.app; EXPO_PUBLIC_* vars must be in .env or shell env at eas update time; demo account in prod DB (credentials kept outside memory).
- [Mobile lru-cache + OTA bundling fix](mobile-lru-cache-fix.md) — lru-cache@5.1.1 must be in root package.json deps to hoist symlink for @babel/helper-compilation-targets; also keep in artifacts/mobile/package.json. Without this, eas update bundling fails with "_lruCache is not a constructor".
- [iOS build provisioning + expo-notifications](ios-provisioning-fix.md) — expo-notifications package auto-adds aps-environment entitlement even without plugin entry; must stay in package.json (removing it breaks Android Gradle). Override with ios.entitlements:{} in app.json to block aps-environment from profile mismatch.
- [Android Gradle build deps](android-gradle-deps.md) — react-native-webview must be 13.15.0 (not 13.12.5) for SDK 54; check with expo install --check before builds.
