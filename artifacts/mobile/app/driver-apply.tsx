import SiteWebView from "@/components/SiteWebView";
import React from "react";

export default function DriverApplyScreen() {
  const uri = "https://flexamarket.com/driver-apply";
  return <SiteWebView uri={uri} />;
}
