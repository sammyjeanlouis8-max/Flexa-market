// Push notifications disabled temporarily — provisioning profile update needed
export function usePushNotifications() {
  // no-op: will be re-enabled after provisioning profile includes aps-environment
}
